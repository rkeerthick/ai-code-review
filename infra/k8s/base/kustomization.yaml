apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-code-review

resources:
  - namespace.yaml
  - api-deployment.yaml
  - worker-deployment.yaml
  - ingress.yaml
  - web-deployment.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: ai-code-review
